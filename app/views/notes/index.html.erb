<h1> Notes </h1>
<p><%= link_to 'Post Note', new_note_path %></p>
<% @notes.each do |note| %>
  <h2><%= note.title %></h2>
  <%= link_to 'Look At Note', note_path(note) %>
  <%= link_to 'Change Note', edit_note_path(note) %>
  <%= link_to 'Remove Note', note, method: :delete %>
<% end %>